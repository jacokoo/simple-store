/// Action Annotation class
class ActionAnnotation { const ActionAnnotation(); }

/// State Annotation class
class StateAnnotation {
    const StateAnnotation();
}
/// Value Annotation class
class ValueAnnotation {
    const ValueAnnotation();
}
/// Page Annotation class
class PageAnnotation {
    const PageAnnotation();
}

/// action annotation
const action = ActionAnnotation();

/// state annotation
const state = StateAnnotation();

/// value annotation
const value = ValueAnnotation();

/// page annotation
const page = PageAnnotation();
